# Kinda Capsule

`kinda_capsule` is an owner-scoped typed episode orchestration layer built on
`kinda_sandbox`. The first vertical slice supports structured command actions,
in-memory traces, observations, and synchronous grading.

It is lifecycle management, not containment. It does not currently promise
durable traces, deterministic replay, artifacts, snapshots, or language and
database action adapters.
